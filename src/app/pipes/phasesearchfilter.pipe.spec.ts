import { PhasesearchfilterPipe } from './phasesearchfilter.pipe';

describe('PhasesearchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PhasesearchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
