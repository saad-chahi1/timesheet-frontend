import { EmployesearchfilterPipe } from './employesearchfilter.pipe';

describe('EmployesearchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EmployesearchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
