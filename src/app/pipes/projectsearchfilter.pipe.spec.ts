import { ProjectsearchfilterPipe } from './projectsearchfilter.pipe';

describe('ProjectsearchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectsearchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
