import { TeamsearchfilterPipe } from './teamsearchfilter.pipe';

describe('TeamsearchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamsearchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
